import 'package:flutter/material.dart';
import '../../../core/widgets/stub_view.dart';

/// mobile layout for the en-IN.TextInfo.ToTitleCase(shop) feature.
/// Replace [StubView] with the real implementation.
class ShopMobileView extends StatelessWidget {
  const ShopMobileView({super.key});

  @override
  Widget build(BuildContext context) =>
      const StubView(featureName: 'en-IN.TextInfo.ToTitleCase(shop)', layoutTier: 'mobile');
}
