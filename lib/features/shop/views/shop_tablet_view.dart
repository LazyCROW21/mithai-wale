import 'package:flutter/material.dart';
import '../../../core/widgets/stub_view.dart';

/// tablet layout for the en-IN.TextInfo.ToTitleCase(shop) feature.
/// Replace [StubView] with the real implementation.
class ShopTabletView extends StatelessWidget {
  const ShopTabletView({super.key});

  @override
  Widget build(BuildContext context) =>
      const StubView(featureName: 'en-IN.TextInfo.ToTitleCase(shop)', layoutTier: 'tablet');
}
