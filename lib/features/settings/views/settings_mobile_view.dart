import 'package:flutter/material.dart';
import '../../../core/widgets/stub_view.dart';

/// mobile layout for the en-IN.TextInfo.ToTitleCase(settings) feature.
/// Replace [StubView] with the real implementation.
class SettingsMobileView extends StatelessWidget {
  const SettingsMobileView({super.key});

  @override
  Widget build(BuildContext context) =>
      const StubView(featureName: 'en-IN.TextInfo.ToTitleCase(settings)', layoutTier: 'mobile');
}
