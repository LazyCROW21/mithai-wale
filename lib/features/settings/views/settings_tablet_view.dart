import 'package:flutter/material.dart';
import '../../../core/widgets/stub_view.dart';

/// tablet layout for the en-IN.TextInfo.ToTitleCase(settings) feature.
/// Replace [StubView] with the real implementation.
class SettingsTabletView extends StatelessWidget {
  const SettingsTabletView({super.key});

  @override
  Widget build(BuildContext context) =>
      const StubView(featureName: 'en-IN.TextInfo.ToTitleCase(settings)', layoutTier: 'tablet');
}
