import 'package:flutter/material.dart';
import '../../../core/widgets/stub_view.dart';

/// desktop layout for the en-IN.TextInfo.ToTitleCase(settings) feature.
/// Replace [StubView] with the real implementation.
class SettingsDesktopView extends StatelessWidget {
  const SettingsDesktopView({super.key});

  @override
  Widget build(BuildContext context) =>
      const StubView(featureName: 'en-IN.TextInfo.ToTitleCase(settings)', layoutTier: 'desktop');
}
