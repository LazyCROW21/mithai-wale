import 'package:flutter/material.dart';
import '../../../core/widgets/stub_view.dart';

/// desktop layout for the en-IN.TextInfo.ToTitleCase(reports) feature.
/// Replace [StubView] with the real implementation.
class ReportsDesktopView extends StatelessWidget {
  const ReportsDesktopView({super.key});

  @override
  Widget build(BuildContext context) =>
      const StubView(featureName: 'en-IN.TextInfo.ToTitleCase(reports)', layoutTier: 'desktop');
}
