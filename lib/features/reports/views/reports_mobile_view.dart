import 'package:flutter/material.dart';
import '../../../core/widgets/stub_view.dart';

/// mobile layout for the en-IN.TextInfo.ToTitleCase(reports) feature.
/// Replace [StubView] with the real implementation.
class ReportsMobileView extends StatelessWidget {
  const ReportsMobileView({super.key});

  @override
  Widget build(BuildContext context) =>
      const StubView(featureName: 'en-IN.TextInfo.ToTitleCase(reports)', layoutTier: 'mobile');
}
