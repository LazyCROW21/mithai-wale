import 'package:flutter/material.dart';
import '../../../core/widgets/stub_view.dart';

/// tablet layout for the en-IN.TextInfo.ToTitleCase(reports) feature.
/// Replace [StubView] with the real implementation.
class ReportsTabletView extends StatelessWidget {
  const ReportsTabletView({super.key});

  @override
  Widget build(BuildContext context) =>
      const StubView(featureName: 'en-IN.TextInfo.ToTitleCase(reports)', layoutTier: 'tablet');
}
