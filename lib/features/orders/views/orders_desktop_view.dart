import 'package:flutter/material.dart';
import '../../../core/widgets/stub_view.dart';

/// desktop layout for the en-IN.TextInfo.ToTitleCase(orders) feature.
/// Replace [StubView] with the real implementation.
class OrdersDesktopView extends StatelessWidget {
  const OrdersDesktopView({super.key});

  @override
  Widget build(BuildContext context) =>
      const StubView(featureName: 'en-IN.TextInfo.ToTitleCase(orders)', layoutTier: 'desktop');
}
