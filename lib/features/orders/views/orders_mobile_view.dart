import 'package:flutter/material.dart';
import '../../../core/widgets/stub_view.dart';

/// mobile layout for the en-IN.TextInfo.ToTitleCase(orders) feature.
/// Replace [StubView] with the real implementation.
class OrdersMobileView extends StatelessWidget {
  const OrdersMobileView({super.key});

  @override
  Widget build(BuildContext context) =>
      const StubView(featureName: 'en-IN.TextInfo.ToTitleCase(orders)', layoutTier: 'mobile');
}
