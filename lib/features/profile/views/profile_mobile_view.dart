import 'package:flutter/material.dart';
import '../../../core/widgets/stub_view.dart';

/// mobile layout for the en-IN.TextInfo.ToTitleCase(profile) feature.
/// Replace [StubView] with the real implementation.
class ProfileMobileView extends StatelessWidget {
  const ProfileMobileView({super.key});

  @override
  Widget build(BuildContext context) =>
      const StubView(featureName: 'en-IN.TextInfo.ToTitleCase(profile)', layoutTier: 'mobile');
}
