import 'package:flutter/material.dart';
import '../../../core/widgets/stub_view.dart';

/// tablet layout for the en-IN.TextInfo.ToTitleCase(profile) feature.
/// Replace [StubView] with the real implementation.
class ProfileTabletView extends StatelessWidget {
  const ProfileTabletView({super.key});

  @override
  Widget build(BuildContext context) =>
      const StubView(featureName: 'en-IN.TextInfo.ToTitleCase(profile)', layoutTier: 'tablet');
}
